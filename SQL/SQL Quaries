CREATE DATABASE sarasavi_db;

USE sarasavi_db;

SET FOREIGN_KEY_CHECKS = 0;


CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    phone VARCHAR(20),
    nic VARCHAR(20) UNIQUE,
    address TEXT,
    role ENUM('user','admin','super_admin') DEFAULT 'user',
    status ENUM('pending','approved','rejected') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
INSERT INTO users (name, email, password, phone, nic, address, role, status) VALUES
('Super Admin', 'superadmin@sarasavi.com', 'super123$', '0771234567', '123456789V', 'Colombo', 'super_admin', 'approved'),
('Admin User', 'admin@sarasavi.com', 'admin123$', '0772345678', '987654321V', 'Galle', 'admin', 'approved'),
('Sathira Pending', 'spending@example.com', 'spending123$', '0774567890', '741258963V', 'Panadura', 'user', 'pending');



CREATE TABLE books (
    b_id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    author VARCHAR(100) NOT NULL,
    cover_image VARCHAR(255)
);



CREATE TABLE bookstock (
    s_id INT AUTO_INCREMENT PRIMARY KEY,
    b_id INT NOT NULL,
    copies INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (b_id) REFERENCES books(b_id) ON DELETE CASCADE
);



CREATE TABLE borrows (
    borrow_id INT AUTO_INCREMENT PRIMARY KEY,
    b_id INT NOT NULL,
    user_id INT NOT NULL,
    borrow_date DATE NOT NULL,
    return_date DATE NOT NULL,
    status ENUM('pending','approved','declined') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (b_id) REFERENCES books(b_id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
);

SET FOREIGN_KEY_CHECKS = 1;